Implement work item **A. Foundation** of the UI/UX refresh for the portfolio site.

WHERE: the git worktree /home/user/ui-refresh (branch `feat/ui-refresh`, already checked out, cut from `fix/pr19-review`, `npm ci` done). Work ONLY there. Never touch /home/user/apanjwani0.com (another process uses it). Commit to `feat/ui-refresh` in the worktree; do NOT push, open PRs, switch branches or touch GitHub. Scratch files: /tmp/claude-0/-home-user-apanjwani0-com/da34734d-9a00-5acf-86dc-099682bd9b1b/scratchpad/item-a/ .

READ FIRST, in full:
1. /home/user/ui-refresh/AGENTS.md — binding rules (token-only styling, shells, CSP nonce, astro check vs build parser traps, security:smoke derived assertions + mutation checks, keep docs in sync).
2. The plan: /tmp/claude-0/-home-user-apanjwani0-com/da34734d-9a00-5acf-86dc-099682bd9b1b/scratchpad/ui-plan.md — read section 1 (audit), section 2 (shared interfaces — you ship ALL of these), section 3 item **A** (your scope, owned files, must-not-touch list, acceptance, assertions with their mutations), section 4 (risks — the Astro 7.3.3 traps: rerouted-404 nonce mismatch, ClientRouter swapping root attributes and re-inserting inline scripts, Oat's color-scheme / light-dark() tokens) and section 5. Items B–G will be implemented later by other workers on top of what you ship, so the interfaces in section 2 must match the plan exactly (names, keys, events, signatures, DOM contract, view-transition names, tokens) — they are a contract.

Decisions already made by the owner (don't re-open): refine the current dark look (not a rebrand); tasteful motion is allowed with prefers-reduced-motion as the off switch (update AGENTS.md's "/frontent-design … no animations" override accordingly); features coming later: ⌘K palette, light/dark/system theme switch, card thumbnails, smart 404 + ? sheet, a toolkit (star tools → My kit, /tools/kit?t=… permalink, bookmarks-file export; only live tools are starrable); the home hero is being chosen separately — do NOT build or change the hero or the avatar (item E does that).

Scope notes:
- Spacing: make the `--space-*` scale strictly increasing in the documented order by swapping the xs/sm NAMES at every call site so rendered pixels are identical (the plan lists the files). Assert monotonicity (mutation: swap back).
- New tokens per section 2, with contrast verified by extending the existing palette-contrast assertions in security-smoke.mjs to every new text pairing (muted on surface-2 etc.) in BOTH themes; if a nudge is needed (e.g. `--color-muted` → ≈#7a8796 in dark, `--color-success` in light), do it and record it in theme.css comments.
- `color-scheme` declared per theme; the Oat token bridge.
- Modules: src/lib/{theme,kit,site-index,fuzzy,shortcuts,site-ui}.ts plus the empty feature stubs (theme-ui, find-ui, kit-ui, motion) that later items fill; `ROOT_BOOT_JS` constant rendered once in Head.astro via `<script is:inline nonce={...} set:html={ROOT_BOOT_JS}>` (byte-identical on every page); `initSiteUI()` called once from both layouts' existing module script; `patchIncomingDocument` wired on astro:before-swap so ClientRouter navigations keep data-theme etc.
- sitemap.xml.ts refactored onto site-index with byte-identical output (assert against the current output across the flag/predicate fixtures the plan lists).
- middleware: the null-body 404/500 CSP guard so rerouted error pages carry a matching nonce (assert at source; add the origin-check.sh probe the plan describes — that script runs against production later, just make it correct).
- security-smoke.mjs: add your assertions (every one mutation-checked: break the guarded code, watch it fail, restore) and create the labelled anchor regions for items B–G the plan describes; AGENTS.md: section stubs for B–G + the Theming / spacing / motion-override updates.
- Keep dark-theme rendering visually identical at 1440 and 390 apart from any deliberate contrast nudge. With a stored `theme:v1` = light preference, pages must render light with no flash on hard load AND after an in-site ClientRouter navigation (there is no toggle UI yet — test by setting localStorage in Playwright).

GATE before every commit: `npm run build`, `npm run check` (0 errors — mind the parser traps in AGENTS.md), `npm run security:smoke`, `npm run poker:check`, `npm run boot:check`. Visual check once with the global Playwright (`import { chromium } from '<npm root -g>/playwright/index.mjs'`; Chromium preinstalled; never `playwright install`): build, then `HOST=127.0.0.1 PORT=4411 node dist/server/entry.mjs` in the background, screenshot /, /tools, /tools/json-tidy, /games, /learnings/which-diagram-to-draw at 1440×900 and 390×844 in dark and in stored-light, compare dark against the same routes on the base commit (build the base in a temp worktree or stash-free approach of your choice), and check the no-flash + ClientRouter navigation case. Kill the server after.

COMMITS: a few logical commits (tokens+spacing; modules+boot script+layout wiring; sitemap refactor; middleware+origin-check; docs). Repo style: lowercase `feat:`/`fix:`/`refactor:` + a plain sentence (see `git log --oneline -15`). End EVERY commit message with exactly:
Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01BSUS4C6dYnTdZArfXVn1Vx

REPORT (≤600 words): what shipped per interface (file paths + exported names so later workers can rely on them), assertions added + the mutation for each, any deviation from the plan and why, gate output, screenshot paths, and `git log --oneline fix/pr19-review..HEAD`.
